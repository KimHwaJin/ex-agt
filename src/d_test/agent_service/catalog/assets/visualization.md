# visualization · 0.1.0

범주별 숫자 합계를 막대 그래프로 비교한다.
`plot_category_totals`는 SVG 문자열을 반환하고 Jupyter에서는 inline 표시한다.
파일 저장과 아티팩트 등록은 하지 않는다. 표본/기간에 따른 해석 한계가 있다.
범주가 너무 많으면 사용하지 않는다. 현재 예시는 최대 30개 범주를 허용한다.

사용 Tool: plot_category_totals@0.1.0
